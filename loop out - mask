//  Animate on the Mask Path
//  Add a Slider Control to the same layer (call it 'Mask Timing')
//  Add two keyframes on the start and end of the animation (0,100)
//  Add expression to the Slider:

loopOut()

//  Add expression to Mask Path:

c=effect("Mask Timing")("Slider").value;
valueAtTime(linear(c,0,100,key(1).time,key(numKeys).time));
